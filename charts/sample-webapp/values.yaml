kubernetesClusterDomain: cluster.local
sampleWebApp:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 5000
  replicas: 1
  type: ClusterIP
  web:
    image:
      repository: nstarx.azurecr.io/nstarxpatch
      tag: samplewebapp_v1
